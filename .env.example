# ===================================================================
# GUI Agent Memory - Configuration Template
# ===================================================================
#
# Instructions:
# 1. Copy this file to a new file named ".env".
# 2. Fill in all REQUIRED values below.
# 3. Optional values can be left as-is to use the defaults from the code,
#    or you can uncomment and modify them to override.
#
# The .env file is ignored by Git and should NOT be committed.
# ===================================================================


# -------------------------------------------------------------------
# LLM Service Endpoints (REQUIRED)
# -------------------------------------------------------------------
# These must be provided for the system to function.
# -------------------------------------------------------------------

# Embedding Model Endpoint
EMBEDDING_LLM_BASE_URL=
EMBEDDING_LLM_API_KEY=

# Reranker Model Endpoint
RERANKER_LLM_BASE_URL=
RERANKER_LLM_API_KEY=

# Experience Distillation Model Endpoint
EXPERIENCE_LLM_BASE_URL=
EXPERIENCE_LLM_API_KEY=


# -------------------------------------------------------------------
# Model Configuration (OPTIONAL)
# -------------------------------------------------------------------

# EMBEDDING_MODEL=Qwen3-Embedding-8B
# RERANKER_MODEL=Qwen3-Reranker-8B
# EXPERIENCE_LLM_MODEL=gpt-4o


# -------------------------------------------------------------------
# Storage & Retrieval (OPTIONAL)
# -------------------------------------------------------------------

# Path to the persistent ChromaDB data directory.
CHROMA_DB_PATH=./memory_system/data/chroma

# Default number of initial candidates to retrieve from vector search.
DEFAULT_TOP_K=20

# Default number of final results to return after reranking.
DEFAULT_TOP_N=3

# The dimension of the embedding vectors. Must match the embedding model.
EMBEDDING_DIMENSION=1024

# Similarity score threshold to trigger the LLM Judge.
SIMILARITY_THRESHOLD_JUDGE=0.80


# -------------------------------------------------------------------
# Logging Configuration (OPTIONAL)
# -------------------------------------------------------------------

# Base directory for all generated log files and artifacts.
LOGS_BASE_DIR=./memory_system/logs

# Enable/disable standard Python logging output (to console/file).
LOG_ENABLED=true

# Set the minimum level for standard Python logging (DEBUG, INFO, WARNING, ERROR, CRITICAL).
LOG_LEVEL=INFO

# Enable/disable the creation of detailed operation artifacts (e.g., prompt files).
# Recommended to set to 'false' in production to save disk space.
OPERATION_LOGS_ENABLED=false


# -------------------------------------------------------------------
# Advanced / Infrastructure Configuration (OPTIONAL)
# -------------------------------------------------------------------

# Disable ChromaDB's anonymous telemetry data collection.
CHROMA_ANONYMIZED_TELEMETRY=false

# Max number of candidates passed to the reranker model.
RERANK_CANDIDATE_LIMIT=20

# Multiplier for hybrid search top_k relative to default_top_k.
HYBRID_TOPK_MULTIPLIER=4

# Default timeout in seconds for all HTTP requests to LLM services.
HTTP_TIMEOUT_SECONDS=10

# --- LLM API Retry Policy ---
LLM_RETRY_ENABLED=true
LLM_RETRY_ATTEMPTS=5
LLM_RETRY_INITIAL_SECONDS=2.0
LLM_RETRY_MAX_SECONDS=30.0


# -------------------------------------------------------------------
# Development & Testing Overrides (FOR ADVANCED USE)
# -------------------------------------------------------------------

# Set to "1" or "true" to force the application to ignore this .env file.
# Useful for ensuring configuration comes ONLY from system environment variables
# in testing or production environments.
# MEMORY_DISABLE_DOTENV=1
